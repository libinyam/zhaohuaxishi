# 朝花夕拾 —— 产品与技术 Spec

> 知乎黑客松 2026 校园新锐季 · 学习工具与知识生产赛道
> 状态：设计共识已收敛（2026-09-11），经三轮外部评审修订，待开发

## 一句话定义

朝花夕拾是一个「收藏夹消化引擎」：在你收藏兴趣的 72 小时黄金期内，把知乎收藏自动拆解成 2 分钟可读完的学习卡片，并通过微信提醒主动推给你。

## 痛点（三个真实死因）

1. **太长了，不想读** → 卡片化：一次 2 分钟，骨架 + 金句
2. **太难了，读不懂** → 讲解层：知乎直答把内容重述成人话，可追问
3. **忘了要看** → 主动触达：Server酱微信推送 + 复习队列

核心洞察：**收藏的兴趣有 72 小时保质期**。刚收藏几天的内容转化率最高；陈年老收藏兴趣已凉透，价值低。
注意：「72 小时」是**我们提出的产品假设/叙事钩子**，不是研究结论——演示话术里必须这样表述（"我们的假设是…"），评委问出处时不夸大。

## 产品结构（两层）

### 钩子层：收藏考古报告（一次性，负责传播）

诊断用户的「收藏衰减曲线」：

- 收藏总数、时间跨度、领域分布
- 收藏 vs 消化比、「最老的一条收藏」
- 「收藏人格」（囤积型/松鼠型/三分钟热度型）——分享主钩子
- 输出 Canvas 海报图，用户可截图分享（自传播闭环）
- 纯元数据计算，零直答消耗

### 日常层：新收藏消化引擎（核心，负责留存）

- 监控新收藏：CLI `me favorites recent`（HTTP：`GET /api/v1/user/collections`），按 `FavTime` 过滤 72h 内
- 黄金期内自动完成拆解炼卡
- 每日推送 3 张（合并为 **1 条** Server酱微信通知）：「你昨晚收藏的《xxx》拆解好了，趁还记得为什么收藏它，花 2 分钟看完」
- 老收藏降级为低频背景消化，按近期活跃领域慢慢清库存

### 多用户边界（评委扫码后能玩到什么）

明确取舍，避免开发中撞墙：

| 能力 | 站主（演示账号） | 评委/访客 |
|---|---|---|
| 浏览示例卡片、考古报告演示 | ✅ | ✅ |
| 看自己账号的考古报告（OAuth 后） | ✅ | ✅（元数据便宜，~10 次 user_data/人） |
| 自己的收藏被炼卡 | ✅ | ❌ **不炼**——保护每日 100 次直答额度，评委看到的是站主的示例卡片 |
| 直答追问 | ✅ | ✅ 限每用户每天 2 次 |
| 每日推送 | ✅（唯一订阅者） | ❌ 不收集评委的 Server酱 SendKey，减少摩擦与隐私负担 |

推送订阅者只有站主一人：SendKey 存服务端环境变量（`SCT_SENDKEY`），不落库、不对访客开放填写。

## 数据边界（已实测，硬约束）

- 官方接口对**他人内容只返回摘要**（~200 字），全文是平台有意的版权边界
- 爬网页补全文 = 在知乎的比赛上违反知乎规则，明确排除
- **本人创作可拿全文**（CLI `me content --content-url`，返回含 HTML 的 `Body` 字段）——已于 9/11 实测验证，演示彩蛋可用
- 数据全部本地缓存，演示不依赖现场网络

### 真实端点对照表（开发以此为准，禁止靠猜）

| 用途 | CLI 命令 | HTTP 端点 | 分页 |
|---|---|---|---|
| 收藏夹列表 | `me favorites lists` | `GET /api/v1/user/favlists` | **无分页**，Limit≤50；收藏夹极多时可能枚举不全（本人实测 1 个，无影响） |
| 收藏夹内容（全量） | `me favorites items --url-token <id>` | `GET /api/v1/user/favlist_contents` | 有分页（`IsEnd`/`NextOffset`），Limit≤50 |
| 近期收藏（增量） | `me favorites recent` | `GET /api/v1/user/collections` | **无分页**，Limit≤50，只适合增量监控，不等于完整历史 |
| 本人创作列表 | `me contents --type all` | `GET /api/v1/user/contents` | 有分页，条目仅 Summary |
| 本人创作正文 | `me content --content-url <url>` | （CLI 封装，文档未列 HTTP 路径） | 返回 `Body` 全文，服务端校验归属 |
| 关注列表 | `me followees` | `GET /api/v1/user/followees` | 有分页 |

**全量拉取策略**：favlists → 逐收藏夹 favlist_contents 分页翻完（本人实测 89 条 = 2 页）。本人账号的私密收藏夹**可以正常读取**（已实测，「公开范围」措辞不影响本人/OAuth 授权场景）。

## 炼卡管线（核心链路，已实测验证可行）

```
收藏条目（标题+摘要+URL 锚点）
  → 知乎直答 zhida-thinking-1p5 定向拆解（服务端 RAG 已读全文，输出论证结构拆解）
  → Gemini 整理成卡片（中转站，OpenAI 兼容协议，凭证走环境变量）
  → 盲审 Agent 门禁（出题考卡片，答不上来打回重炼）
  → 人工终审（作者本人品味把关）
```

已验证：直答能对指定问题输出「核心观点 + 四层内容框架 + 论证手法」级的真实拆解。

**来源错位声明与 UX 处理**：直答命中的是该问题下的优质讨论，不保证恰好是收藏的那一条。卡片的 `source.url` 指向用户收藏的具体回答，而拆解可能来自同问题下另一条回答——必须在 UI 上显式呈现这个错位：卡片左上角加角标「拆解自该问题下的优质讨论」，角标点击/悬停展开说明「AI 拆解综合了该问题下多篇高赞回答，不局限于你收藏的这条；点击下方链接阅读你收藏的原回答」。**宁可自己声明，不能被评委抓包。**

分工铁律：**直答提供事实（唯一能合法读知乎全文的途径，不可替换为 Gemini 裸调，否则拆解沦为幻觉）；Gemini 提供手艺（卡片格式化与盲审，不接触知乎原文内容）。**

### 卡片 JSON Schema（联调契约，三道工序与复习队列都以此对齐）

```json
{
  "id": "card_809952554",
  "source": {
    "contentType": "answer",
    "title": "各位大佬能帮我全面了解一下九州系列吗？",
    "url": "https://www.zhihu.com/answer/809952554",
    "authorName": "温酒",
    "favTime": 1779179511,
    "likeCount": 839
  },
  "coreView": "九州系列没有必读的阅读顺序，按作者选主线即可；入门首推《九州缥缈录》。",
  "thread": [
    { "step": "首推阶段完结作品", "detail": "推荐派以《缥缈录》为核心，文笔稳重、有明确走向，是最好的入口" },
    { "step": "建立分析模型", "detail": "引入「物理规则→物理现象→社会行为」链条，用结构模型诊断世界观" },
    { "step": "诊断规则断裂", "detail": "九州缺乏中间映射层，创作者痴迷表层争霸，压缩了叙事空间" },
    { "step": "肯定情感价值", "detail": "即使设定有缺陷，吕归尘、姬野等人物依然带来强烈共鸣" }
  ],
  "keyInsight": "",
  "points": [
    "系列整体无强制顺序，仅个别作者（如江南）内部有三部曲顺序",
    "大量短篇仅在杂志发表过，现已难寻，不建议强求",
    "想补设定看《创造古卷》，但注意其不被所有九州元老认可"
  ],
  "quote": "只有具体到某个作者，才会有顺序问题。",
  "difficulty": "easy",
  "topicTags": ["九州", "奇幻文学", "阅读指南"],
  "status": "pending_review",
  "reviewScore": null,
  "nextReviewAt": 1789133746,
  "reviewCount": 0,
  "createdAt": 1789133746
}
```

字段类型约定：

- `thread` 讲解脉络：3-5 步 `{step, detail}`（step≤12 字，detail≤60 字），还原讲解推进顺序。**理科/知识类**必须按「问题 → 直觉 → 关键技巧 → 严格论证 → 应用与局限」推进，公式一律保留 LaTeX；**观点/讨论类**还原观点交锋与论证结构
- `keyInsight` 关键洞察（为什么成立/证明思路/技巧，≤100 字可含 LaTeX）：理科类必填，纯观点类可空字符串
- `points` 固定 3 条 string；`difficulty` 枚举 `easy|medium|hard`
- `status` 枚举：`pending_review`（待审）→ `approved`（过审，进队列）/ `rejected`（淘汰）/ `summary_only`（降级，仅摘要展示不进队列）；`approved` 卡片完成第 3 次复习后转 `digested`（已消化，出队）
- `reviewScore` 盲审得分（0-5），未审为 null
- `nextReviewAt` 下次复习时间戳，`reviewCount` 已复习次数——复习队列算法依赖这两个字段

### 盲审门禁判定标准

- 盲审 Agent 基于**缓存的直答拆解原文**（不重新请求，零额度消耗）出 5 道题：3 道事实回忆 + 2 道理解应用
- 用卡片内容作答，**≥4/5 通过**
- 不通过则打回重炼（消耗 1 次直答额度），**最多打回 2 次**；仍不过则卡片降级为 `summary_only`
- 全部判定记录落盘，演示话术可引用通过率

### 复习队列算法（MVP 简化版间隔重复）

- 依赖字段：`nextReviewAt` + `reviewCount`（见 Schema）
- 复习间隔：第 1 次后 +1 天，第 2 次后 +3 天，第 3 次后 +7 天并转 `digested` 出队
- 每日推送选卡优先级：**72h 内新收藏的卡 > 到期复习卡 > 老收藏补位**（老收藏按「用户近 30 天有行为（新收藏/新创作）的领域优先 + FavTime 倒序」）
- 每日推送数 = `min(3, 按优先级取到的卡片数)`，不足 3 张不硬凑

### 推送与调度

- 调度器：Node 进程内 `node-cron`（单容器即可，不依赖 Sealos 外部 cron 能力），每日 08:00 触发「选卡 → Server酱推送」
- Server酱限额：免费版 **5 条/天**（每日 3 张合并 1 条，够用）；**新用户 7 天全功能试用，现在注册正好覆盖 9/13-15 演示期**（来源：[Server酱官方 FAQ](https://sct.ftqq.com/docs/getting-started/faq/)）
- Server酱失败处理：指数退避重试 3 次（1s/5s/25s），仍失败记入死信日志；**推送只是提醒，卡片内容在页面内拉取，推送失败不影响当日学习**

### 追问限流实现

- 用户标识：匿名访客首次访问种 UUID（cookie + localStorage 双写）；OAuth 用户用知乎 uid 覆盖
- 计数存服务端 JSON（`data/quota/<yyyy-mm-dd>.json`），按日重置；每用户每天 2 次，超限返回「今日额度已用完，明天再来」
- 追问先查缓存（规范化问题文本做 key），命中不烧额度

## 存储设计（「落盘缓存」铁律的具体形态）

全部用**扁平 JSON 文件**（不引 SQLite——数据量小、零依赖、可直接肉眼检查、git 可 diff）：

```
data/
├── favorites.json            # 全量收藏快照（含 FavTime，考古报告数据源）
├── review-queue.json         # 复习队列状态
├── quota/<yyyy-mm-dd>.json   # 每日追问计数
└── cache/
    ├── zhida/<contentKey>.json   # 直答拆解原文缓存（铁律：同一收藏永不重复请求）
    ├── cards/<cardId>.json       # 炼好的卡片
    └── answers/<hash>.json       # 追问缓存
```

- 去重键 `contentKey`：规范化后的内容 URL（剥掉 utm 参数）取主体 ID（如 `answer/809952554` → `809952554`）
- 写缓存一律先写临时文件再 rename，防止中断写坏
- 开发调试模式读 `cache/` 回放，不发真实请求

## 技术栈

- 底座：官方脚手架 server.mjs（vanilla Node，不动，doctor 要过）
- 前端：vanilla JS + Tailwind CDN，无构建步骤
- 模型：Gemini 中转站（OpenAI 兼容 /v1/chat/completions），批量炼卡用 Flash 级，盲审用 Pro 级；凭证经环境变量注入（`GEMINI_BASE_URL` / `GEMINI_API_KEY`），不进对话、不进代码
- 推送：Server酱（微信服务通知）
- 部署：Sealos（公网 HTTPS，国内低延迟，9/13 后兼作 OAuth 回调域）
- 账号：开发期用本人 Access Secret（已配置）；9/13 拿到 OAuth app_id/app_key 后支持评委扫码玩自己的账号

## 验收标准（可检查的门槛）

- **9/13 00:00 前**：89 条收藏全量落盘；≥20 张卡片通过盲审+人工终审；考古报告本地可跑通
- **9/14 12:00 前**：Sealos 部署上线；OAuth 联调通过（凭证到位前提下）；Server酱实测推送成功 ≥1 次
- **演示前最终 checklist**：20 张过审卡片就绪；考古报告海报可生成；追问缓存 ≥10 条；当日直答额度剩余 ≥20 次；断网模式下完整演示流程走通 1 次

## 时间表

- **9/11-9/12（预开发）**：拉全 89 条收藏建本地数据集；打通直答拆解 → Gemini 炼卡 → 盲审管线；卡片质量达标
- **9/13-9/15（48h 冲刺）**：界面（报告页 + 卡片流 + 复习队列）、考古报告海报、Server酱接入、Sealos 部署、OAuth 接入（凭证到位后）、演示打磨

## 演示叙事

1. 亮你的真实收藏考古报告（笑点 + 共鸣）
2. 抛出观点：「我们的假设是——收藏有 72 小时保质期」（明确标注为产品假设，非研究结论）
3. 现场演示一条新收藏的拆解卡片 + 直答追问（卡片角标主动展示「拆解自该问题下的优质讨论」）
4. 手机当场收到微信提醒
5. 评委扫码玩（OAuth 就绪后）

关键话术：「我们严格只用官方开放能力——卡片负责激活兴趣，原文回流知乎，对平台是正向循环。」「我们把每日 100 次直答额度当稀缺资源设计：全部落盘缓存，同一收藏永不重复请求。」

## 配额架构原则（面向所有终端用户）

Access Secret 是**开发者凭证**——网站被多少人用，所有 API 调用都从开发者账号的额度池扣。OAuth 解决「读谁的数据」，不改变「烧谁的额度」。因此产品必须预生成为主、实时生成为辅：

| 操作 | 额度消耗 | 设计 |
|---|---|---|
| 看已炼好的卡片 / 考古报告 | 0（读本地缓存） | 任意多用户，0 边际成本 |
| 新用户首次授权读收藏夹 | ~10 次 user_data/人 | 额度 1 万/天，宽裕 |
| 直答追问 | 1 次/问 | 限流：每用户每天 2 次，超限提示「今日额度已用完，明天再来」；先查缓存 |

演示日预算验算：30 名评委/观众 × 2 次追问 = 60 次 + 现场演示预留 20 次 = 80 次 < 100 次 ✅

## 配额预算（硬约束，已核实）

官方邀测额度（每日重置，同一账号所有 Access Secret 共享额度池，多申请 Secret 无法绕过）：

| 能力 | 每日额度 | 我们的用途 |
|---|---|---|
| **知乎直答** | **100 次** | 拆解炼卡 + 卡片追问（核心瓶颈） |
| 用户数据 | 10000 次 | 拉收藏夹/创作，宽裕 |
| 知乎搜索 | 5000 次 | 宽裕 |
| 热榜 | 100 次 | 备用 |

**直答预算分配**（100 次/天是核心管线的生死线）：

- **9/11（今天，剩 98 次）**：预炼 50 条收藏（50 次拆解），留 48 次余量（其中约 10 次预计消耗在管线调试上）
- **9/12**：炼完剩余 39 条 + 盲审打回的重炼，总消耗控制在 90 次内
- **9/13-15（冲刺+演示日）**：每日预留 20 次给现场拆解和评委追问，其余用于补充炼卡
- **铁律：所有直答响应落盘缓存，同一收藏永不重复请求**；追问也先查缓存
- 考古报告纯用元数据，零直答消耗
- 开发调试一律用已缓存的响应回放，不烧额度

## 风险与对策

| 风险 | 对策 |
|---|---|
| **直答每日 100 次额度卡死核心管线** | 预炼分摊到 9/11-9/12 两天（共 200 次额度）；所有响应落盘缓存；演示日每日预留 20 次；盲审重炼上限 2 次 |
| **Gemini 中转站故障或限速** | 开工首日实测并发与限额；降级方案：跳过格式化，直答拆解原文直接渲染为卡片，盲审暂停改全人工终审；核心演示不依赖 Gemini 在线 |
| 现场网络/接口不稳 | 全部数据预缓存，现场可离线演示 |
| 直答对部分小众问题拆不出货 | 盲审门禁拦截 + 降级 `summary_only` + 人工筛出 20 条高质量卡片保底 |
| 卡片质量不稳定 | 盲审 + 人工终审；只演示通过终审的卡片 |
| Server酱推送失败或超限 | 免费版 5 条/天（每日合并 1 条推送）；新用户 7 天全功能试用覆盖演示期；指数退避重试 3 次 + 死信日志；推送仅提醒，内容页内拉取不受影响 |
| OAuth 凭证 9/13 才发放 | 主体功能不依赖 OAuth，登录是加分项；评委的收藏不炼卡（保额度），只看报告+示例卡片 |
| 撞题（知识星图等） | 差异化已定：他们做「看见」，我们做「消化」；判据是已消化数真实增加 |
